name: Extreme Movers Screener

on:
  # Ausloesung erfolgt zuverlaessig extern ueber cron-job.org (siehe README.md),
  # kein eigener schedule-Trigger (siehe Begruendung in alerts.yml/gold.yml).
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  movers-check:
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Abhaengigkeiten installieren
        run: pip install -r requirements.txt

      - name: S&P-500-Ausreisser pruefen und ggf. senden
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python movers_screener.py

      - name: State-Datei zurueckschreiben
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add movers_state.json
          git diff --staged --quiet || git commit -m "Update movers state [skip ci]"
          git push
